<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AIDevGallery.Pages.ScenarioSelectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <NavigationView
        x:Name="NavView"
        AutomationProperties.AutomationId="ScenarioNavView"
        CompactModeThresholdWidth="0"
        ExpandedModeThresholdWidth="1000"
        IsBackButtonVisible="Collapsed"
        IsPaneToggleButtonVisible="True"
        IsPaneVisible="True"
        IsSettingsVisible="False"
        OpenPaneLength="276"
        PaneDisplayMode="Auto"
        SelectionChanged="NavView_SelectionChanged">
        <NavigationView.Resources>
            <Thickness x:Key="NavigationViewContentGridBorderThickness">1,0,0,0</Thickness>
            <CornerRadius x:Key="NavigationViewContentGridCornerRadius">0,0,0,0</CornerRadius>
            <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
            <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
        </NavigationView.Resources>
        <NavigationView.PaneHeader>
            <Grid Margin="16,12,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Samples" />
                <StackPanel
                    Grid.Column="1"
                    Margin="0,0,4,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal"
                    Spacing="2">
                    <FontIcon
                        AutomationProperties.AccessibilityView="Raw"
                        FontSize="14"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                        Glyph="&#xE71C;" />
                    <ComboBox
                        AutomationProperties.Name="Filters"
                        DisplayMemberPath="Text"
                        ItemsSource="{x:Bind filters}"
                        SelectedIndex="0"
                        SelectedValuePath="Tag"
                        SelectionChanged="ComboBox_SelectionChanged"
                        Style="{StaticResource SubtleComboBoxStyle}" />
                </StackPanel>
            </Grid>
        </NavigationView.PaneHeader>
        <NavigationView.Content>
            <Frame x:Name="NavFrame" />
        </NavigationView.Content>
    </NavigationView>
</Page>
